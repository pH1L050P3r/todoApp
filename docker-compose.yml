version: '3.1'

services:
  web:
    build: .
    command:
      [
        "./build/app.o"
      ]
    ports:
      - "8080:8000"
    depends_on:
      - db

  db:
    image: postgres:14
    env_file: .env
    ports:
      - 5432:5432
    volumes:
      - ./db-data:/var/lib/postgresql/data
